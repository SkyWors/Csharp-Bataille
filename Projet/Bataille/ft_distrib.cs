using System;
using System.Collections.Generic;
using System.Threading;

namespace Bataille
{
    internal class ft_distrib
    {
        public static void distrib(Stack<int> pile, int slot)
        {
            //Random random = new Random();
            //int entier;
            //for (int i = 0; i < 10; i++)
            //{
            //    entier = random.Next(1, 50);
            //    pile.Push(entier);
            //}
            //Thread.Sleep(10); //This is a debug for the fucking random function who doesn't work when is call at the same timestamp.
        }
    }
}
